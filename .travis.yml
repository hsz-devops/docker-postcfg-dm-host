---
language: python
python:
    - 2.7

#sudo: required
#
#services:
#  - docker

before_install:
  - pip2 install --upgrade pip setuptools wheel docker-compose
  - docker-compose --version

scripts:
  - make build
