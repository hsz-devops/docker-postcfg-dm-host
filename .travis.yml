---
language: python
python:
    - 2.7

sudo: required

services:
 - docker

install:
  - pip2 install --upgrade pip setuptools wheel docker-compose
  - docker-compose --version

script:
  - make build
